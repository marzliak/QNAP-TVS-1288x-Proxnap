# Add only mappings validated by observing one physical bay at a time.
# Accepted LED keys: hdd1..hdd8, ssd1..ssd4, m2ssd1, m2ssd2.
# Values are whole-device kernel names, not partitions. Examples are omitted
# deliberately because enumeration varies with controllers and cabling.
poll_seconds=0.25
activity_hold_seconds=0.75
smart_interval_seconds=300
